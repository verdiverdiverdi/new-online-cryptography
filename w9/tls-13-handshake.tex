\documentclass{standalone}
\usepackage{tikz}
\usepackage{tikzpeople}
\begin{document}
\begin{tikzpicture}
		\tikzset{
			cylinder/.style={draw,
				shape=cylinder,
				name=nodename, % Can be defined arbitrarily
				alias=cyl, % Will be used by the ellipse to reference the cylinder
				aspect=1.5,
				minimum height=8.5cm,
				minimum width=2.25cm,
				color=blue,
				fill= blue!30,
				outer sep=-0.5\pgflinewidth, % to make sure the ellipse does not draw over the lines
				%shape border rotate=90
		}}
		
		\draw[draw=black,fill=black, opacity=0.1] (2,8) rectangle  (12,0);
		\node[] (HSKLBL) at (7,8.25) {Handshake Protocol};
		\node[alice, monitor, label= Client Alice, minimum size=1.5cm] (Alice) at (0,4) {};
		\node[bob, mirrored, monitor, label=Server Bob, minimum size=1.5cm] (Bob) at (14,4) {};
		
		{\draw[->, ultra thick] (2,7.5)  -- (12,7.5) node[midway, above] {\texttt{CHello:} $r_A$, $g^x$};}
		{\draw[<-, ultra thick] (2,6.5)  -- (12,6.5) node[midway, above] {\texttt{SHello:}, $r_B$, $g^y$};}
		{\node[] (KDF) at (7,5.85) {$k_h, k_f, k_a = \mathsf{KDF}(g^{xy}, r_A \Vert r_B)$};}
		{
			\draw[draw=black,fill=green, opacity=0.1] (2,5.6) rectangle  (12,0);}
		{\draw[<-, ultra thick] (2,5)  -- (12,5) node[midway, above] {\texttt{SCert}: $\mathsf{Enc}(k_h, (pk_B$, Cert$_B))$};}
		{\draw[<-, ultra thick] (2,4.25)  -- (12,4.25) node[midway, above] {\texttt{SCertVfy}: $\mathsf{Enc}(k_h, \mathsf{Sign}(sk_B,H($transcript$_B)))$};}
		{\draw[<-, ultra thick] (2,3.5)  -- (12,3.5) node[midway, above] {\texttt{SFin}: $\mathsf{Enc}(k_h, \mathsf{MAC}(k_f,H($transcript'$_B)))$};}
		{\draw[->, ultra thick] (2,2.5)  -- (12,2.5) node[midway, above] {\texttt{CCert}$^*$: $\mathsf{Enc}(k_h,(pk_A$, Cert$_A))$};}
		{\draw[->, ultra thick] (2,1.75)  -- (12,1.75) node[midway, above] {\texttt{CCertVfy}$^*$: $\mathsf{Enc}(k_h, \mathsf{Sign}(sk_A,H($transcript$_A)))$};}
		{\draw[->, ultra thick] (2,1)  -- (12,1) node[midway, above] {\texttt{CFin}: $\mathsf{Enc}(k_h, \mathsf{MAC}(k_f,H($transcript'$_A)))$};}
\end{tikzpicture}
\end{document}
